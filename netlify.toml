# https://docs.netlify.com/configure-builds/file-based-configuration/
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback (safe even if you add client-side routes later)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
