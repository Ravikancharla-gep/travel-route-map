# Used automatically by `vite build` (production). Safe to commit — public API URL only.
# For Netlify Git builds, override with Netlify env if you use a different API host.
VITE_API_URL=https://route-map-india-api.onrender.com/api
